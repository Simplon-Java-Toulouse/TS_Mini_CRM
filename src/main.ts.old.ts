/*
let employee: IEmployee[] = [];
let c1 = new Company(employee, "Noukio", "Toulouse", "http://noukio.fr", 31000);
let e1 = new Employee(c1, "lorem ipsum", "michel@drucker.com", "Michel", "Drucker", "0661667788");
let e2 = new Employee(c1, "lorem ipsum", "joseline@drucker.com", "Joseline", "Drucker", "0661667788");
employee.push(e1);
employee.push(e2);
console.log(c1);
console.log(e1);
*/

/*
for (let i = 0; i < 0; i++) {
    let li = document.createElement('li');
    li.innerHTML = `${c1.employee[i].first_name} ${c1.employee[i].last_name}`;
    ul.appendChild(li);
}
*/

/*
app.innerHTML = `${c1.name} - ${c1.region} - ${c1.zipCode}, <a href=${c1.website}>siteweb</a>`;
app.appendChild(ul);
*/
